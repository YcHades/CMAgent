# LLM配置示例文件 - OpenAI 风格统一接口
# API_KEY和BASE_URL优先使用本配置文件的，未指定模型将使用通过 .env 文件加载的环境变量

output_dir: ./output

llm:
  gpt-4o:
    model: gpt-4o
  
  gpt-5:
    model: gpt-5

  qwen3-14b:
    model: Qwen3-14B
    base_url: http://127.0.0.1:8101/v1